@model LibreriaDeClases.Modelos.Categoria

@{
    ViewData["Title"] = "Details";
}

<h1>Detalle</h1>

<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
</div>
<div>
    @if (ViewBag.EsAdministrador == true)
    {
        <a asp-action="Edit" asp-route-id="@Model?.IdCategoria">Editar</a>
        <text> | </text>
    }
    <a asp-action="Index">Volver a index</a>
</div>
